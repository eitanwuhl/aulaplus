# Workspace Class Planning – Diff vs Commit 9e87845984f1db916008cb19f203a04c7a30e832

## 1. Overview

Este documento analiza las diferencias entre el commit `9e87845984f1db916008cb19f203a04c7a30e832` (fijado como "punto de comparación") y el código actual (`HEAD`) en el workspace de planificación de clases.

### Commit de Referencia
- **Hash**: `9e87845984f1db916008cb19f203a04c7a30e832`
- **Mensaje**: "fix(planificacion): impedir recursos en contenido de sesiones sin período"
- **Fecha**: (commit reciente en el historial del proyecto)

### Cambios Principales Detectados

Entre el commit de referencia y el código actual, se identificaron los siguientes cambios significativos en la UI de planificación de clases:

1. **Eliminación del parser de plan estructurado** (`planParser.ts`)
2. **Cambio en la renderización de secciones** (de secciones explícitas a HTML directo)
3. **Cambios en la visibilidad de recursos** (de tabs separados a integración directa)
4. **Reordenamiento de secciones** en el layout del editor de sesión

---

## 2. Lesson Sections (Inicio / Desarrollo / Cierre)

### 2.1 Previous Behavior (commit 9e8784…)

**Archivos involucrados:**
- `src/lib/planParser.ts` (EXISTÍA en el commit anterior, ELIMINADO en actual)
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 45-680)

**Estructura y estilo:**

En la versión anterior, las secciones del plan de clase se manejaban mediante un **parser dedicado** que:

1. **Parseaba el HTML** generado por la IA usando `parsePlan(planHtml, recursos)`:
   ```typescript
   const planParsed = useMemo(() => {
     if (!planHtml) {
       return { inicio: '', desarrollo: '', cierre: '', recursos: recursos, diferenciacion: undefined, durations: undefined };
     }
     return parsePlan(planHtml, recursos);
   }, [planHtml, recursos]);
   ```

2. **Separaba explícitamente** el contenido en tres secciones estructuradas:
   - `inicio`: HTML limpio de la sección de inicio (sin recursos embebidos)
   - `desarrollo`: HTML limpio de la sección de desarrollo
   - `cierre`: HTML limpio de la sección de cierre
   - `diferenciacion`: Sección opcional extraída por separado
   - `durations`: Duraciones extraídas de los encabezados (ej: "(15 min)")

3. **Renderizaba cada sección** con su propio encabezado visual:
   ```typescript
   {hasHtml(planParsed?.inicio) && (() => {
     const inicioHtml = stripLeadingDuplicateSectionHeading(
       sanitizeHeadings(planParsed!.inicio),
       'inicio'
     );

     return (
       <div>
         <h2 className="scroll-m-20 text-xl font-semibold tracking-tight mt-6 mb-3">
           Inicio{planParsed?.durations?.inicio ? ` ${planParsed.durations.inicio}` : ''}
         </h2>
         <div
           className="space-y-4 leading-relaxed"
           dangerouslySetInnerHTML={{ __html: inicioHtml }}
         />
       </div>
     );
   })()}
   ```

4. **Separadores visuales** entre secciones:
   ```typescript
   {hasHtml(planParsed?.inicio) && hasHtml(planParsed?.desarrollo) && (
     <div role="separator" className="border-t border-border my-6" />
   )}
   ```

5. **Funciones auxiliares de limpieza**:
   - `sanitizeHeadings()`: Eliminaba agresivamente encabezados duplicados de secciones
   - `stripLeadingDuplicateSectionHeading()`: Quitaba encabezados iniciales duplicados en cada sección
   - `demoteInternalHeadings()`: Convertía encabezados h1-h6 internos a `<p><strong>` para mantener jerarquía limpia
   - `hoistInlineDiferenciacion()`: Extraía bloques de diferenciación embebidos en el contenido

**Estilo visual:**
- Cada sección tenía un encabezado H2 con estilo específico:
  - `text-xl font-semibold tracking-tight mt-6 mb-3`
  - Color: `text-primary` para destacar secciones
- Separadores visuales (`border-t border-border my-6`) entre cada sección
- Espaciado vertical consistente (`space-y-4 leading-relaxed`)

### 2.2 Current Behavior

**Archivos involucrados:**
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 1-516)
- `src/lib/planHtmlNormalizer.ts` (normalización básica de encabezados)

**Estructura y estilo:**

En la versión actual:

1. **NO existe parser estructurado** - El archivo `planParser.ts` fue eliminado

2. **Renderizado directo del HTML**:
   ```typescript
   <TabsContent value="clase" className="space-y-4">
     <div 
       className="w-full prose max-w-none p-6 bg-card rounded-lg border 
         prose-headings:font-bold prose-headings:font-black
         prose-h1:text-2xl prose-h1:font-black prose-h1:mt-8 prose-h1:mb-4
         prose-h2:text-xl prose-h2:font-bold prose-h2:mt-6 prose-h2:mb-4 prose-h2:text-primary
         prose-h3:text-lg prose-h3:font-semibold prose-h3:mt-4 prose-h3:mb-3
         prose-p:mb-4 prose-p:leading-relaxed
         prose-ul:mb-4 prose-ul:ml-4
         prose-li:mb-2
         prose-strong:font-bold prose-strong:text-primary"
       dangerouslySetInnerHTML={{ __html: planHtml }}
     />
   </TabsContent>
   ```

3. **Sin separación explícita de secciones** - El HTML completo se renderiza como un bloque único usando clases de Tailwind Typography (`prose`)

4. **Normalización mínima** - Solo se usa `normalizePlanHeadings()` para asegurar que los `<h2>` tengan `<strong>` dentro:
   ```typescript
   export function normalizePlanHeadings(html: string): string {
     if (!html) return '';
     const h2WithoutStrong = /<h2>((?!<strong>).+?)<\/h2>/gis;
     const normalized = html.replace(
       h2WithoutStrong, 
       '<h2><strong>$1</strong></h2>'
     );
     return normalized;
   }
   ```

5. **Sin separadores visuales** entre secciones (depende del HTML generado por la IA)

6. **Estilo con Tailwind Typography** - Se confía en las clases `prose-*` para dar formato consistente al HTML generado

### 2.3 Key Code Differences

**Archivos modificados:**

1. **`src/lib/planParser.ts`**
   - ❌ **ELIMINADO completamente** en la versión actual
   - Este archivo contenía ~577 líneas de lógica de parsing estructurado

2. **`src/components/planificacion/EditorSesionNuevo.tsx`**
   - ❌ Líneas 45-51: Eliminado `useMemo` que llamaba a `parsePlan()`
   - ❌ Líneas 53-241: Eliminadas funciones auxiliares de limpieza (`stripTags`, `hasHtml`, `normalize`, `isSectionTitle`, `sanitizeHeadings`, `demoteInternalHeadings`, `stripLeadingDuplicateSectionHeading`)
   - ❌ Líneas 593-683: Eliminado renderizado estructurado por secciones con encabezados y separadores
   - ✅ Líneas 377-389: Agregado renderizado directo con clases `prose`

3. **`src/lib/planHtmlNormalizer.ts`**
   - ✅ **NUEVO archivo** con normalización básica (solo 40 líneas)
   - Reemplaza la funcionalidad compleja del parser con una función simple

**Diferencias funcionales:**

| Aspecto | Versión Anterior (9e8784) | Versión Actual (HEAD) |
|---------|---------------------------|----------------------|
| **Parser estructurado** | ✅ Sí (`planParser.ts`) | ❌ No |
| **Separación de secciones** | ✅ Explícita (inicio/desarrollo/cierre) | ❌ Implícita (depende del HTML generado) |
| **Encabezados visuales** | ✅ Generados por el componente | ⚠️ Dependen del HTML de la IA |
| **Separadores visuales** | ✅ Sí (`border-t` entre secciones) | ❌ No |
| **Limpieza de HTML** | ✅ Extensa (múltiples funciones) | ⚠️ Mínima (`normalizePlanHeadings`) |
| **Extracción de duraciones** | ✅ Sí (parseadas del HTML) | ❌ No |
| **Hoisting de diferenciación** | ✅ Sí (extraída de las secciones) | ❌ No |

### 2.4 Effort to Restore Old Behavior

**Complejidad estimada: MEDIA-ALTA**

**Pasos necesarios:**

1. **Restaurar `planParser.ts`** (o recrearlo):
   - ✅ El archivo existe en el commit anterior, se puede recuperar con `git show`
   - Requiere importar en `EditorSesionNuevo.tsx`
   - Dependencias: `normalizeArrayField` de `normalizeSupabaseArrays.ts`

2. **Modificar `EditorSesionNuevo.tsx`**:
   - Agregar el `useMemo` que parsea el plan (línea 46-51 de la versión anterior)
   - Agregar funciones auxiliares de limpieza (líneas 53-241)
   - Reemplazar el renderizado actual (líneas 377-389) con el renderizado estructurado (líneas 593-683)
   - Agregar importación de `parsePlan` y `buildPlanHtml`

3. **Restaurar funciones de modificación con parser**:
   - En `handleSolicitarModificacion` (líneas 268-340 del commit anterior)
   - En `handleGenerarPlanInicial` (líneas 342-426)
   - Usar `parsePlan()` y `buildPlanHtml()` para sanitizar el HTML antes de guardar

4. **Actualizar el tab de Recursos**:
   - Usar `planParsed.recursos` en lugar de `recursos` directo
   - Mostrar recursos parseados del HTML en lugar de recursos guardados en DB

**Justificación de complejidad:**

- ✅ **A favor**: El código ya existe en el commit anterior, solo hay que restaurarlo
- ⚠️ **Contra**: Requiere cambios en múltiples archivos y reintroducir lógica compleja
- ⚠️ **Contra**: Puede haber conflictos con otros cambios hechos en el código actual
- ⚠️ **Contra**: Requiere testing extensivo para asegurar que el parsing funcione correctamente

**Tiempo estimado**: 3-5 horas de desarrollo + 2-3 horas de testing

---

## 3. Resources Visibility (Clase vs Recursos)

### 3.1 Previous Behavior (commit 9e8784…)

**Archivos involucrados:**
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 833-875)

**Comportamiento:**

En la versión anterior:

1. **Recursos NO aparecían en la pestaña "Clase"**:
   - El parser (`parsePlan`) **extraía recursos del HTML** y los separaba del contenido narrativo
   - Las funciones `extractAndCleanResources()` y `extractResourceItems()` limpiaban el HTML de cualquier mención de recursos
   - Solo se mostraban las secciones de inicio/desarrollo/cierre SIN recursos embebidos

2. **Recursos aparecían SOLO en la pestaña "Recursos"**:
   ```typescript
   <TabsContent value="recursos" className="space-y-4">
     <Card>
       <CardHeader>
         <CardTitle>Recursos Necesarios</CardTitle>
         <CardDescription>
           Materiales y recursos requeridos para esta clase
         </CardDescription>
       </CardHeader>
       <CardContent>
         {planParsed.recursos && planParsed.recursos.length > 0 ? (
           <ul className="list-disc pl-6 space-y-1">
             {planParsed.recursos.map((recurso, index) => (
               <li key={index} className="text-sm">{recurso}</li>
             ))}
           </ul>
         ) : (
           <p className="text-sm text-muted-foreground italic">
             No se han especificado recursos para esta clase.
           </p>
         )}
       </CardContent>
     </Card>
   ```

3. **Sección adicional para recursos manuales**:
   ```typescript
   <Card className="border-l-4 border-amber-500 bg-amber-50 dark:bg-amber-950/20">
     <CardHeader>
       <CardTitle className="text-lg">Recursos adicionales (opcionales)</CardTitle>
       <CardDescription>
         Agrega recursos extra que no fueron detectados automáticamente. 
         Estos NO aparecerán en la pestaña Clase.
       </CardDescription>
     </CardHeader>
     <CardContent className="space-y-4">
       <Textarea
         value={recursos.join('\n')}
         onChange={(e) => setRecursos(e.target.value.split('\n').filter(Boolean))}
         className="min-h-[150px]"
         placeholder="Un recurso por línea (ej: Pizarra, Marcadores, Proyector)..."
       />
       <Button onClick={handleGuardarRecursos}>Guardar Recursos Adicionales</Button>
     </CardContent>
   </Card>
   ```

4. **Recursos parseados del HTML**:
   - Los recursos mencionados en el HTML generado por la IA se extraían automáticamente
   - Se mostraban en la sección "Recursos Necesarios" dentro del tab "Recursos"
   - Se normalizaban y deduplicaban usando `mergeResources()` y `normalizeResource()`

**Flujo de datos:**
```
IA genera HTML → parsePlan() extrae recursos → recursos se limpian del HTML narrativo → 
recursos se muestran SOLO en tab "Recursos" → recursos adicionales se guardan por separado
```

### 3.2 Current Behavior

**Archivos involucrados:**
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 489-511)

**Comportamiento:**

En la versión actual:

1. **Recursos PUEDEN aparecer en la pestaña "Clase"**:
   - El HTML se renderiza directamente sin parsing
   - Si la IA incluye recursos en el HTML (ej: `<p><strong>Recursos:</strong> Pizarra, marcadores</p>`), estos se muestran en la pestaña "Clase"
   - No hay limpieza automática de bloques de recursos

2. **Pestaña "Recursos" simplificada**:
   ```typescript
   <TabsContent value="recursos" className="space-y-4">
     <Label>Recursos necesarios</Label>
     <Textarea
       value={recursos.join('\n')}
       onChange={(e) => setRecursos(e.target.value.split('\n').filter(Boolean))}
       className="min-h-[200px]"
       placeholder="Un recurso por línea..."
     />
     <Button onClick={handleGuardarRecursos}>Guardar Recursos</Button>
   </TabsContent>
   ```

3. **Recursos manuales únicamente**:
   - No hay extracción automática de recursos del HTML
   - Solo se manejan los recursos guardados manualmente en el array `recursos` (de la DB)
   - No hay card de "Recursos Necesarios" que muestre recursos parseados

**Flujo de datos:**
```
IA genera HTML → HTML se renderiza directamente en tab "Clase" (puede incluir recursos) → 
recursos manuales se guardan en tab "Recursos" (array separado en DB)
```

**Consecuencia:**
- ❌ Duplicación: Los recursos pueden aparecer tanto en "Clase" (si la IA los incluyó en el HTML) como en "Recursos" (si el docente los agregó manualmente)
- ❌ Inconsistencia: No hay una fuente única de verdad para los recursos de una clase

### 3.3 Key Code Differences

**Archivos modificados:**

1. **`src/components/planificacion/EditorSesionNuevo.tsx` - Tab "Clase"**:
   - ❌ **Anterior**: HTML limpio sin recursos (después de `extractAndCleanResources()`)
   - ⚠️ **Actual**: HTML completo con posibles recursos embebidos

2. **`src/components/planificacion/EditorSesionNuevo.tsx` - Tab "Recursos"**:
   - ❌ **Anterior** (líneas 833-875):
     - Card principal con recursos parseados del HTML
     - Card secundaria con recursos adicionales manuales
     - Mensaje explícito: "Estos NO aparecerán en la pestaña Clase"
   - ⚠️ **Actual** (líneas 489-511):
     - Solo textarea para recursos manuales
     - Sin separación entre recursos parseados y manuales

3. **`src/lib/planParser.ts`**:
   - ❌ **Anterior**: Funciones `extractAndCleanResources()`, `extractResourceItems()`, `mergeResources()`, `normalizeResource()`
   - ❌ **Actual**: No existe el archivo

**Diferencias funcionales:**

| Aspecto | Versión Anterior (9e8784) | Versión Actual (HEAD) |
|---------|---------------------------|----------------------|
| **Extracción automática de recursos** | ✅ Sí (desde HTML) | ❌ No |
| **Recursos en tab "Clase"** | ❌ No (limpiados del HTML) | ⚠️ Sí (si la IA los incluyó) |
| **Recursos en tab "Recursos"** | ✅ Sí (parseados + manuales) | ⚠️ Solo manuales |
| **Normalización de recursos** | ✅ Sí (deduplicación, limpieza) | ❌ No |
| **Separación clara** | ✅ Sí (dos cards: parseados vs manuales) | ❌ No |
| **Mensaje de instrucción** | ✅ "NO aparecerán en Clase" | ❌ Sin instrucción |

### 3.4 Effort Assessment

**Complejidad estimada: BAJA-MEDIA**

**Pasos necesarios:**

1. **Restaurar `planParser.ts`**:
   - Ya cubierto en la sección 2.4
   - Las funciones de extracción de recursos ya están en ese archivo

2. **Modificar tab "Clase"**:
   - Usar `planParsed.inicio`, `planParsed.desarrollo`, `planParsed.cierre` en lugar de `planHtml` directo
   - Esto garantiza que los recursos se hayan limpiado del HTML narrativo

3. **Modificar tab "Recursos"**:
   - Restaurar la estructura de dos cards (líneas 833-875 del commit anterior)
   - Primera card: Mostrar `planParsed.recursos` (recursos parseados del HTML, solo lectura)
   - Segunda card: Textarea para recursos adicionales manuales (actual)
   - Agregar descripción clara: "Agrega recursos extra que no fueron detectados automáticamente. Estos NO aparecerán en la pestaña Clase."

4. **Sincronizar recursos en guardar**:
   - Al guardar, combinar recursos parseados + recursos manuales usando `mergeResources()`
   - Guardar el resultado en la DB (campo `recursos`)

**Justificación de complejidad:**

- ✅ **A favor**: Una vez restaurado `planParser.ts`, la implementación es directa
- ✅ **A favor**: El código ya existe en el commit anterior
- ⚠️ **Contra**: Requiere coordinar con la restauración de secciones (punto 2)
- ⚠️ **Contra**: Puede haber confusión si el docente agrega manualmente recursos que ya están parseados

**Tiempo estimado**: 1-2 horas de desarrollo + 1 hora de testing

**Alternativa más simple** (sin restaurar parser completo):
- Agregar una función client-side que busque patrones de recursos en el HTML usando regex
- Limpiar esos bloques antes de renderizar en tab "Clase"
- Extraerlos y mostrarlos en tab "Recursos"
- **Ventaja**: No requiere restaurar todo el parser
- **Desventaja**: Menos robusto que el parser completo

**Tiempo estimado (alternativa)**: 2-3 horas de desarrollo + 1 hora de testing

---

## 4. "Diferenciación/Adaptaciones" Position

### 4.1 Previous Behavior (commit 9e8784…)

**Archivos involucrados:**
- `src/lib/planParser.ts` (función `hoistInlineDiferenciacion`)
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 664-679)

**Comportamiento:**

En la versión anterior:

1. **Extracción inteligente de diferenciación**:
   - La función `hoistInlineDiferenciacion()` detectaba bloques de diferenciación embebidos en cualquier sección (inicio/desarrollo/cierre)
   - Patrón de detección: `/<p[^>]*>\s*<strong>\s*(?:diferenciación|adaptaciones)\s*:?\s*<\/strong>\s*<\/p>/i`
   - Extraía el contenido hasta el siguiente encabezado o bloque de recursos
   - Limpiaba el contenido original para evitar duplicación

2. **Renderizado al FINAL del tab "Clase"**:
   ```typescript
   {(hasHtml(planParsed?.inicio) || hasHtml(planParsed?.desarrollo) || hasHtml(planParsed?.cierre)) &&
     hasHtml(planParsed?.diferenciacion) && (
       <div role="separator" className="border-t border-border my-6" />
     )}

   {hasHtml(planParsed?.diferenciacion) && (
     <div>
       <h2 className="scroll-m-20 text-xl font-semibold tracking-tight mt-6 mb-3">
         Diferenciación/Adaptaciones
       </h2>
       <div
         className="space-y-4 leading-relaxed"
         dangerouslySetInnerHTML={{ __html: sanitizeHeadings(planParsed!.diferenciacion!) }}
       />
     </div>
   )}
   ```

3. **Orden de renderizado explícito**:
   - Inicio (con su separador)
   - Desarrollo (con su separador)
   - Cierre (con su separador)
   - **Diferenciación/Adaptaciones** ← AL FINAL

4. **Separador visual antes de diferenciación**:
   - Solo se mostraba si había contenido previo (inicio/desarrollo/cierre)
   - Línea horizontal (`border-t`) con margen vertical (`my-6`)

5. **Encabezado consistente**:
   - Título fijo: "Diferenciación/Adaptaciones"
   - Estilo idéntico a otros encabezados de sección (`text-xl font-semibold`)

**Estructura JSX:**
```jsx
<div className="space-y-8">
  {/* Inicio */}
  {hasHtml(planParsed?.inicio) && <Inicio />}
  
  {/* Separador */}
  {hasHtml(planParsed?.inicio) && hasHtml(planParsed?.desarrollo) && <Separator />}
  
  {/* Desarrollo */}
  {hasHtml(planParsed?.desarrollo) && <Desarrollo />}
  
  {/* Separador */}
  {hasHtml(planParsed?.desarrollo) && hasHtml(planParsed?.cierre) && <Separator />}
  
  {/* Cierre */}
  {hasHtml(planParsed?.cierre) && <Cierre />}
  
  {/* Separador antes de diferenciación */}
  {(hasMain) && hasHtml(planParsed?.diferenciacion) && <Separator />}
  
  {/* Diferenciación - AL FINAL */}
  {hasHtml(planParsed?.diferenciacion) && <Diferenciacion />}
</div>
```

### 4.2 Current Behavior

**Archivos involucrados:**
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 377-389)
- `supabase/functions/generate-plan-completo/index.ts` (prompt de la IA)

**Comportamiento:**

En la versión actual:

1. **Sin extracción de diferenciación**:
   - No existe la función `hoistInlineDiferenciacion()`
   - El HTML se renderiza tal como viene de la IA

2. **Posición dependiente del prompt de la IA**:
   - En el prompt actual (líneas 82-86 de `generate-plan-completo/index.ts`):
     ```typescript
     <p><strong>Diferenciación/Adaptaciones:</strong></p>
     <ul>
       <li>Adaptación para perfil visual</li>
       <li>Adaptación para perfil auditivo</li>
     </ul>
     ```
   - La IA puede colocar "Diferenciación/Adaptaciones" en CUALQUIER lugar:
     - Dentro de "Inicio" (como sugiere el prompt ejemplo)
     - Dentro de "Desarrollo"
     - Al final de "Cierre"
     - Como sección separada

3. **Sin control sobre posición**:
   - El componente renderiza el HTML directamente sin reordenar
   - No hay lógica que mueva la diferenciación al final

4. **Sin encabezado consistente**:
   - Puede aparecer como `<p><strong>Diferenciación/Adaptaciones:</strong></p>` (párrafo)
   - O como `<h2><strong>Diferenciación/Adaptaciones</strong></h2>` (encabezado)
   - O cualquier otra variación que la IA decida

**Estructura JSX:**
```jsx
<TabsContent value="clase" className="space-y-4">
  <div 
    className="w-full prose max-w-none p-6 bg-card rounded-lg border"
    dangerouslySetInnerHTML={{ __html: planHtml }}  // ← Todo junto, sin control de orden
  />
</TabsContent>
```

### 4.3 Key Code Differences

**Archivos modificados:**

1. **`src/lib/planParser.ts`**:
   - ❌ **Anterior**: Función `hoistInlineDiferenciacion()` (líneas 22-69)
     - Detectaba bloques de diferenciación en cualquier parte del HTML
     - Los extraía del contenido principal
     - Los devolvía como campo separado en `ParsedPlan`
   - ❌ **Actual**: No existe

2. **`src/components/planificacion/EditorSesionNuevo.tsx`**:
   - ❌ **Anterior** (líneas 664-679):
     - Renderizado explícito de diferenciación AL FINAL
     - Separador visual antes de la sección
     - Encabezado H2 consistente
   - ⚠️ **Actual** (líneas 377-389):
     - Renderizado directo del HTML completo
     - Sin control sobre posición de diferenciación

3. **Procesamiento de diferenciación en parser** (anterior):
   ```typescript
   // Post-process each section to hoist inline diferenciacion blocks
   const diffs: string[] = [];

   const r1 = hoistInlineDiferenciacion(inicio);
   inicio = r1.cleanedHtml;
   if (r1.extracted) diffs.push(r1.extracted);

   const r2 = hoistInlineDiferenciacion(desarrollo);
   desarrollo = r2.cleanedHtml;
   if (r2.extracted) diffs.push(r2.extracted);

   const r3 = hoistInlineDiferenciacion(cierre);
   cierre = r3.cleanedHtml;
   if (r3.extracted) diffs.push(r3.extracted);

   // Merge hoisted diferenciacion with any existing diferenciacion section
   let diferenciacionHtml = diffs.join('\n');
   ```

**Diferencias funcionales:**

| Aspecto | Versión Anterior (9e8784) | Versión Actual (HEAD) |
|---------|---------------------------|----------------------|
| **Extracción de diferenciación** | ✅ Automática (de cualquier sección) | ❌ No |
| **Posición de diferenciación** | ✅ Siempre al final | ⚠️ Depende de la IA |
| **Encabezado consistente** | ✅ H2 con estilo uniforme | ⚠️ Variable según IA |
| **Separador visual** | ✅ Sí (antes de la sección) | ❌ No (o depende del HTML) |
| **Deduplicación** | ✅ Sí (solo una sección final) | ❌ Puede aparecer múltiples veces |
| **Control de layout** | ✅ Completo (código JSX) | ❌ Ninguno (HTML directo) |

### 4.4 Effort Assessment

**Complejidad estimada: BAJA**

**Pasos necesarios:**

**Opción A: Restaurar parser completo** (recomendado si se hace el punto 2)

1. **Restaurar `planParser.ts`**:
   - Ya cubierto en sección 2.4
   - La función `hoistInlineDiferenciacion()` ya está incluida

2. **Usar `planParsed.diferenciacion`**:
   - Al renderizar el tab "Clase", agregar al final:
     ```tsx
     {hasHtml(planParsed?.diferenciacion) && (
       <>
         <div role="separator" className="border-t border-border my-6" />
         <div>
           <h2 className="scroll-m-20 text-xl font-semibold tracking-tight mt-6 mb-3">
             Diferenciación/Adaptaciones
           </h2>
           <div
             className="space-y-4 leading-relaxed"
             dangerouslySetInnerHTML={{ __html: planParsed!.diferenciacion! }}
           />
         </div>
       </>
     )}
     ```

**Opción B: Solución independiente sin parser** (si NO se restaura el parser)

1. **Crear función de reordenamiento en `EditorSesionNuevo.tsx`**:
   ```typescript
   function reorderDiferenciacionToEnd(html: string): { main: string; diferenciacion: string } {
     // Detectar bloques de diferenciación/adaptaciones
     const headerRe = /<(?:h2|p)[^>]*>\s*<strong>\s*(?:diferenciaci[oó]n|adaptaciones)\s*:?\s*<\/strong>\s*<\/(?:h2|p)>/i;
     const match = html.match(headerRe);
     
     if (!match) {
       return { main: html, diferenciacion: '' };
     }
     
     const start = match.index ?? 0;
     const end = html.indexOf('<h2', start + match[0].length); // Siguiente sección
     const diferenciacionBlock = end > 0 
       ? html.slice(start, end) 
       : html.slice(start);
     
     const mainHtml = html.slice(0, start) + (end > 0 ? html.slice(end) : '');
     
     return { main: mainHtml.trim(), diferenciacion: diferenciacionBlock.trim() };
   }
   ```

2. **Aplicar al renderizar**:
   ```tsx
   const { main, diferenciacion } = useMemo(() => reorderDiferenciacionToEnd(planHtml), [planHtml]);
   
   // En el JSX:
   <div dangerouslySetInnerHTML={{ __html: main }} />
   {diferenciacion && (
     <>
       <div className="border-t border-border my-6" />
       <div dangerouslySetInnerHTML={{ __html: diferenciacion }} />
     </>
   )}
   ```

**Justificación de complejidad:**

- ✅ **A favor**: Cambio relativamente aislado (solo afecta el renderizado final)
- ✅ **A favor**: Opción B no requiere restaurar todo el parser
- ⚠️ **Contra Opción A**: Requiere restaurar parser (ya cubierto en punto 2)
- ⚠️ **Contra Opción B**: Regex puede fallar si la IA usa formato inesperado

**Tiempo estimado**:
- **Opción A** (con parser): 30 minutos (si ya se restauró el parser en punto 2)
- **Opción B** (sin parser): 1-2 horas de desarrollo + 1 hora de testing

**Recomendación**:
- Si se restaura el parser (punto 2), usar **Opción A** (trivial)
- Si NO se restaura el parser, usar **Opción B** (independiente pero más frágil)

---

## 5. Competencies in Class Content Header

### 5.1 Previous Behavior (commit 9e8784…)

**Archivos involucrados:**
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 514-560)

**Comportamiento:**

En la versión anterior, las competencias **YA SE MOSTRABAN** en el header de la clase:

```typescript
<Card>
  <CardHeader>
    <CardTitle className="text-2xl">{contenidoPrincipal}</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
      <div>
        <span className="text-muted-foreground">Fecha:</span>
        <p className="font-semibold">
          {sesion.fecha 
            ? new Date(sesion.fecha).toLocaleDateString('es-ES', { day: 'numeric', month: 'long' })
            : 'Fecha: a confirmar'}
        </p>
      </div>
      <div>
        <span className="text-muted-foreground">Duración:</span>
        <p className="font-semibold flex items-center gap-1">
          <Clock className="h-4 w-4" />
          {sesion.duracion_minutos} min
        </p>
      </div>
      <div>
        <span className="text-muted-foreground">Materia:</span>
        <p className="font-semibold">{materia || 'Sin especificar'}</p>
      </div>
      <div>
        <span className="text-muted-foreground">Nivel:</span>
        <p className="font-semibold">{nivel || 'Sin especificar'}</p>
      </div>
    </div>
    <div className="mt-4">
      <span className="text-muted-foreground text-sm">Competencias:</span>
      <div className="flex gap-2 mt-2 flex-wrap">
        {sesion.competencias_anep && sesion.competencias_anep.length > 0 ? (
          sesion.competencias_anep.map((comp, i) => (
            <Badge key={i} variant="secondary">{comp}</Badge>
          ))
        ) : (
          <span className="text-sm text-muted-foreground italic">
            No hay competencias específicas para esta sesión
          </span>
        )}
      </div>
    </div>
  </CardContent>
</Card>
```

**Estructura del header:**

1. **Título principal**: Contenido ANEP de la sesión (línea 517)
2. **Grid de metadatos** (4 columnas):
   - Fecha
   - Duración
   - Materia
   - Nivel
3. **Sección de competencias** (líneas 545-558):
   - Label: "Competencias:"
   - Badges con cada competencia de `sesion.competencias_anep`
   - Mensaje si no hay competencias

**Fuente de datos:**
- `sesion.competencias_anep`: Array de strings con las competencias de la sesión (viene de la DB)

### 5.2 Current Behavior

**Archivos involucrados:**
- `src/components/planificacion/EditorSesionNuevo.tsx` (líneas 298-344)

**Comportamiento:**

En la versión actual, las competencias **TAMBIÉN SE MUESTRAN** en el header:

```typescript
<Card>
  <CardHeader>
    <CardTitle className="text-2xl">{contenidoPrincipal}</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
      <div>
        <span className="text-muted-foreground">Fecha:</span>
        <p className="font-semibold">
          {sesion.fecha 
            ? new Date(sesion.fecha).toLocaleDateString('es-ES', { day: 'numeric', month: 'long' })
            : 'Fecha: a confirmar'}
        </p>
      </div>
      <div>
        <span className="text-muted-foreground">Duración:</span>
        <p className="font-semibold flex items-center gap-1">
          <Clock className="h-4 w-4" />
          {sesion.duracion_minutos} min
        </p>
      </div>
      <div>
        <span className="text-muted-foreground">Materia:</span>
        <p className="font-semibold">{materia || 'Sin especificar'}</p>
      </div>
      <div>
        <span className="text-muted-foreground">Nivel:</span>
        <p className="font-semibold">{nivel || 'Sin especificar'}</p>
      </div>
    </div>
    <div className="mt-4">
      <span className="text-muted-foreground text-sm">Competencias:</span>
      <div className="flex gap-2 mt-2 flex-wrap">
        {sesion.competencias_anep && sesion.competencias_anep.length > 0 ? (
          sesion.competencias_anep.map((comp, i) => (
            <Badge key={i} variant="secondary">{comp}</Badge>
          ))
        ) : (
          <span className="text-sm text-muted-foreground italic">
            No hay competencias específicas para esta sesión
          </span>
        )}
      </div>
    </div>
  </CardContent>
</Card>
```

**Diferencia:**
- ✅ **IGUAL**: La estructura es **IDÉNTICA** en ambas versiones
- ✅ Las competencias se muestran de la misma forma en el header

### 5.3 Key Code Differences

**Ninguna diferencia detectada en este aspecto.**

La funcionalidad de mostrar competencias en el header del contenido de la clase **ya existe** tanto en el commit anterior como en la versión actual.

**Código idéntico:**
- Mismo componente `Card` con misma estructura
- Mismo grid de metadatos
- Misma sección de competencias con badges
- Misma fuente de datos: `sesion.competencias_anep`

### 5.4 Effort Assessment

**Complejidad estimada: N/A (ya implementado)**

**Conclusión:**

Las competencias **YA SE MUESTRAN** en el header de la clase en ambas versiones. No hay trabajo pendiente en este aspecto.

**Posibles mejoras (opcionales):**

1. **Mostrar descripción extendida de competencias**:
   - Actualmente solo se muestra el ID/código de la competencia
   - Se podría expandir para mostrar la descripción completa al hacer hover o clic
   - Requeriría cargar datos adicionales de competencias (de `competencias.ts` o `competenciasCiudadania.ts`)

2. **Filtrar competencias del período**:
   - Actualmente se muestran las competencias específicas de la sesión (`sesion.competencias_anep`)
   - Se podría agregar un indicador visual de cuáles son competencias del período completo
   - Requeriría comparar con `competenciasDelPeriodo` (ya disponible como prop)

3. **Agregar selector de competencias**:
   - Permitir al docente modificar las competencias de la sesión desde el header
   - Requeriría integrar el `CompetenceSelector` existente

**Tiempo estimado para mejoras opcionales**: 2-4 horas

---

## 6. Summary & Recommendations

### 6.1 Resumen de Cambios Detectados

| Aspecto | Estado en 9e8784 | Estado Actual | Cambio |
|---------|------------------|---------------|--------|
| **1. Secciones estructuradas** | ✅ Parser dedicado | ❌ HTML directo | 🔴 Pérdida de funcionalidad |
| **2. Recursos aislados** | ✅ Solo en tab "Recursos" | ⚠️ Pueden aparecer en "Clase" | 🟡 Inconsistencia |
| **3. Diferenciación al final** | ✅ Posición controlada | ⚠️ Depende de la IA | 🟡 Pérdida de control |
| **4. Competencias en header** | ✅ Implementado | ✅ Implementado | 🟢 Sin cambios |

### 6.2 Recomendaciones por Área

#### **Área 1: Lesson Sections (Inicio / Desarrollo / Cierre)**

**Recomendación: RESTAURAR**

**Motivo:**
- La separación visual de secciones mejora significativamente la legibilidad
- Los encabezados consistentes ayudan al docente a orientarse en el plan
- Los separadores visuales crean una estructura clara
- La limpieza del HTML evita redundancias y duplicaciones

**Riesgo de no restaurar:**
- El HTML generado por la IA puede ser inconsistente
- Las secciones pueden fusionarse o no tener separación visual clara
- Duplicación de encabezados (la IA puede incluir "Desarrollo" en el HTML interno)

**Alternativa:**
- Si restaurar el parser completo es demasiado costoso, implementar una versión simplificada con regex
- Buscar solo los encabezados principales (Inicio/Desarrollo/Cierre) y reemplazarlos con componentes React
- Menos robusto pero más rápido de implementar

#### **Área 2: Resources Visibility**

**Recomendación: RESTAURAR (con prioridad alta)**

**Motivo:**
- **Duplicación actual es confusa**: Los recursos pueden aparecer en dos lugares
- **Inconsistencia de datos**: No hay fuente única de verdad
- **UX pobre**: El docente no sabe dónde agregar o ver recursos

**Riesgo de no restaurar:**
- Confusión del usuario sobre dónde agregar recursos
- Duplicación de información (recursos en HTML + recursos manuales)
- Dificultad para generar PDF o exportar (no se sabe qué recursos incluir)

**Implementación recomendada:**
1. Restaurar extracción automática de recursos del HTML
2. Mostrar recursos parseados en tab "Recursos" (solo lectura)
3. Permitir agregar recursos adicionales en sección separada
4. Al guardar, combinar recursos parseados + manuales (con deduplicación)
5. Limpiar recursos del HTML narrativo antes de mostrar en tab "Clase"

**Tiempo**: 2-3 horas (depende de si se restaura parser completo o versión simplificada)

#### **Área 3: Diferenciación/Adaptaciones Position**

**Recomendación: RESTAURAR (prioridad media)**

**Motivo:**
- La posición al final es una convención pedagógica
- Facilita al docente encontrar rápidamente las adaptaciones
- Evita que la diferenciación se pierda en medio del contenido
- Mejora la estructura lógica del documento

**Riesgo de no restaurar:**
- La IA puede colocar diferenciación en lugares inesperados
- Puede aparecer múltiples veces (dentro de cada sección)
- El docente puede no encontrar fácilmente las adaptaciones

**Implementación recomendada:**
- Si se restaura el parser: Trivial (30 minutos)
- Si NO se restaura el parser: Implementar función de reordenamiento independiente (2 horas)

**Prioridad**: Media (mejora la UX pero no es crítico)

#### **Área 4: Competencies in Header**

**Recomendación: MANTENER COMO ESTÁ**

**Motivo:**
- Ya está implementado correctamente
- No hay cambios entre versiones
- Funciona bien en ambos casos

**Mejoras opcionales** (baja prioridad):
- Mostrar descripción completa al hover
- Indicar competencias del período vs sesión
- Permitir editar desde el header

**Tiempo**: 2-4 horas (si se implementan mejoras opcionales)

### 6.3 Plan de Acción Sugerido

**Fase 1: Quick Wins (2-4 horas)**
- ✅ Restaurar aislamiento de recursos (Área 2 con versión simplificada)
  - Crear función de extracción de recursos con regex
  - Limpiar recursos del HTML antes de mostrar en tab "Clase"
  - Mostrar recursos extraídos en tab "Recursos"

**Fase 2: Structural Improvements (4-6 horas)**
- ✅ Restaurar parser completo o versión simplificada (Área 1)
  - Decidir: ¿parser completo o versión simplificada con regex?
  - Implementar separación de secciones
  - Agregar encabezados y separadores visuales
- ✅ Restaurar posición de diferenciación (Área 3)
  - Se beneficia del parser restaurado en Área 1
  - O implementar reordenamiento independiente

**Fase 3: Polish & Testing (2-3 horas)**
- ✅ Testing exhaustivo de parsing en casos edge
- ✅ Verificar que recursos no se dupliquen
- ✅ Verificar que diferenciación siempre esté al final
- ✅ Testing visual en diferentes tamaños de pantalla

**Tiempo total estimado: 8-13 horas**

### 6.4 Dependencias y Riesgos

**Dependencias:**
- Área 3 (Diferenciación) depende idealmente de Área 1 (Parser)
- Si se restaura el parser completo, todas las áreas se benefician

**Riesgos:**
1. **Conflictos con código actual**:
   - El parser puede entrar en conflicto con cambios posteriores al commit 9e8784
   - Mitigación: Review cuidadoso del diff completo antes de integrar

2. **Cambios en edge function**:
   - Si el prompt de la IA cambió significativamente, el parser puede no funcionar
   - Mitigación: Testing exhaustivo con HTML generado por la IA actual

3. **Regresiones en otras funcionalidades**:
   - Restaurar código antiguo puede afectar funcionalidades nuevas
   - Mitigación: Testing completo de todo el flujo de generación y edición

4. **Performance**:
   - El parser agrega overhead al renderizado
   - Mitigación: Usar `useMemo` para cachear parsing

### 6.5 Alternativa: No Restaurar (Status Quo)

Si se decide NO restaurar ninguna funcionalidad:

**Ventajas:**
- ✅ Menos trabajo de desarrollo
- ✅ Menos riesgo de regresiones
- ✅ Código más simple (menos lógica de parsing)

**Desventajas:**
- ❌ UX inconsistente (recursos pueden duplicarse)
- ❌ Pérdida de estructura visual clara
- ❌ Dependencia total del formato que genere la IA
- ❌ Dificultad para mantener coherencia pedagógica

**Recomendación final: Restaurar al menos Área 2 (Recursos) y Área 1 (Secciones simplificadas)**

---

## Apéndice: Archivos Clave

### Archivos modificados entre 9e8784 y HEAD:
1. `src/lib/planParser.ts` ❌ **ELIMINADO**
2. `src/lib/planHtmlNormalizer.ts` ✅ **NUEVO** (reemplazo simplificado)
3. `src/components/planificacion/EditorSesionNuevo.tsx` 🔄 **MODIFICADO** (reducido de ~893 a ~516 líneas)
4. `src/pages/PlanificacionWorkspace.tsx` 🔄 **MODIFICADO** (agregada lógica de generación automática)

### Archivos a restaurar (si se acepta la recomendación):
1. `src/lib/planParser.ts` (recuperar del commit 9e8784)

### Archivos a modificar:
1. `src/components/planificacion/EditorSesionNuevo.tsx`
2. Potencialmente `supabase/functions/generate-plan-completo/index.ts` (para mejorar prompt)

---

**Documento generado el**: {{ fecha actual }}  
**Commit analizado**: `9e87845984f1db916008cb19f203a04c7a30e832`  
**Commit actual**: `HEAD`  
**Autor del análisis**: Claude (Asistente IA)































